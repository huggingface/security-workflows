name: security-validate
on: 
  pull_request:
  workflow_dispatch:
permissions: 
  contents: write
  pull-requests: write
jobs:
  validate:
    uses: huggingface/deny-actions-registry/.github/workflows/validate.yml@main
